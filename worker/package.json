{
  "name": "@letters/worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "test": "node --test --test-reporter=spec",
    "db:local": "wrangler d1 migrations apply letters-in-the-ocean --local",
    "db:remote": "wrangler d1 migrations apply letters-in-the-ocean --remote",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "4.127.1"
  }
}
